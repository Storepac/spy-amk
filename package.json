{
  "name": "amk-spy-api",
  "version": "1.0.0",
  "description": "API para sincronização de dados do AMK Spy com MongoDB",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed for serverless functions'"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.12"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
